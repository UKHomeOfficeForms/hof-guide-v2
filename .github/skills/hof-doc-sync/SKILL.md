---
name: hof-doc-sync
description: "Use when: syncing HOF Guide content after hof framework changes, changelog updates, release diffs, or repository_dispatch sync tasks."
argument-hint: "Paste HOF diff context JSON or issue link"
---

# HOF Guide sync skill

Use this skill to update `hof-guide-v2` from real changes in `UKHomeOfficeForms/hof`.

## When to use

- A framework change lands in `hof` (`master`/`main`)
- A release or changelog update introduces behaviour changes
- A sync issue is raised by the `hof-guide-sync-from-hof` workflow

## Inputs expected

1. Sync issue link (preferred)
2. `.tmp/hof-sync/context.json` generated by workflow
3. Changelog notes and compare URL

## Procedure

1. Read the sync issue and the generated context payload.
2. Validate the code diff against the listed docs impact areas.
3. If the context says no guide update is required, do not open a PR and report that back on the issue.
4. Update only impacted pages in this guide.
5. Ensure examples stay aligned with framework behaviour (not assumptions).
6. Use a single conventional commit for the actual documentation change. Do not create an `initial plan` commit.
7. Ensure the PR body includes `Closes #<sync-issue-number>`.
8. Run `yarn build` and resolve any regressions.
9. Prepare a PR-ready summary with:
   - what changed in framework terms
   - what changed in guide pages
   - any follow-up gaps requiring human product decisions

## Update checklist

Follow [update checklist](./references/update-checklist.md).
